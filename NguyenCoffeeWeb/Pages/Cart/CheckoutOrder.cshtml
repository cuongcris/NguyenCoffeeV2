@page
@model NguyenCoffeeWeb.Pages.Cart.CheckoutOrderModel
@{
}
@{
    ViewData["Title"] = "Checkout Order";
}

<h2 class="text-center mt-3">Chi tiết đơn hàng</h2>

@if (Model.OrderBill != null)
{
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Name</th>
                <th>Quantity</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var order in Model.OrderBill.CoffeeOrderList)
            {
                <tr>
                    <td>@order.Name</td>
                    <td>@order.Quantity</td>
                </tr>
            }
        </tbody>
        <tfoot>
            <tr >
                <div class="text-center">
                    @if (Model.OrderBill.ImagePath != null)
                    {
                        <img src="@Model.OrderBill.ImagePath" alt="Order Bill Image" />
                    }
                </div>
            </tr>
            <tr>
                <th>Tổng số lượng</th>
                <th>@Model.OrderBill.CoffeeOrderList.Sum(o => o.Quantity)</th>
            </tr>
            <tr>
                <th>Total Bill</th>
            </tr>
        </tfoot>
    </table>
    <div class="text-center mb-5">
        <form method="post">
            <input type="hidden" asp-for="@Model.OrderBill.ImagePath" />
            <button class="btn btn-primary" type="submit" value="">Gửi Order</button>
        </form>
    </div>
}
else
{
    <h3 class="text-center mt-5 mb-5" style="">@ViewData["EmptyOrder"]</h3>
    <h3 class="text-center mt-5 mb-5" style="color:green">@ViewData["OrderSuccess"]</h3>
    <div class="text-center">
        <a class=" btn btn-primary mb-5" value="" asp-page="/Menu">Quay về Menu</a>
    </div>
}